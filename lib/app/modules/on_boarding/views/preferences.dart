import 'package:flutter/material.dart';

class PreferenciView extends StatelessWidget {
  const PreferenciView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('PreferenciView'),
      ],
    );
  }
}
